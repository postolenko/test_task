$(window).load(function() {
  $(".logos_slide").mCustomScrollbar({
    axis:"x",
    scrollButtons:{
      enable: true,
      scrollType: "continuous",
      scrollSpeed: 20,
      scrollAmount: 40
    }
  });
});
